spring:
  application:
    name: servicediscovery

server:
  port: 8761

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false


logging.level.com.netflix:
  eureka: INFO
  discovery: INFO
