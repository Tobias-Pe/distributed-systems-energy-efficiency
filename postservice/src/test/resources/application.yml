spring:
  datasource:
    username: sa
    password: sa
    url: jdbc:h2:mem:db
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: create
  properties:
    hibernate:
      format_sql: true